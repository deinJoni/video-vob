# PACKAGE + ITERATE improvement PRDs

Seven implementation-ready PRDs for phases **8 (PACKAGE)** and **9 (ITERATE)** of the FSM. Authored against the real engine seams, adversarially reviewed for invariant safety, and flagged for parallel-safety against the concurrent phase 1–7 refactors.

**Fixed product decisions:** Wave A (5) + thin ITERATE capture (2). ITERATE stays thin (archive read-back + structured revision capture + labels; no parallel A/B model). Captions + distribution + loudness mirror into the `import-deliverable` fan-out path; posters + multi-aspect stay package-only.

## Build order

| # | PRD | Wave | Parallel-safe w/ 1–7 | Notes |
|---|-----|------|----------------------|-------|
| 01 | [Chunk-level caption sidecars (.srt + .vtt) at PACKAGE and import](PRD-01-caption-sidecar.md) | A | ✅ yes | shares mcp/lib/caption-sidecar.js with its own import mirror |
| 02 | [Poster set: generalize the single thumbnail to N output-time frames](PRD-02-poster-set.md) | A | ✅ yes | none |
| 03 | [Platform distribution metadata as a typed PLAN layer surfaced at PACKAGE + import](PRD-03-distribution-metadata.md) | A | ✅ yes | none (storyboarder populates target.distribution — skill layer) |
| 04 | [vob_compare_iterations — read-only cross-version diff over archived snapshots](PRD-04-compare-iterations.md) | A | ✅ yes | UX prerequisite for promote-iteration |
| 05 | [Multi-aspect dumb-crop escape hatch (opt-in, labeled lossy)](PRD-05-multi-aspect-crop.md) | A | ✅ yes | none |
| 06 | [Structured revision capture at the back-edge + ratable version labels](PRD-06-iteration-feedback-and-labels.md) | B | ⚠️ no (engine-heart) | capture + labels ship together (shared inputSchema) |
| 07 | [Promote an archived cut via the existing back-edge (not a render-slot writer)](PRD-07-promote-iteration.md) | B | ⚠️ no (engine-heart) | needs compare-iterations (UX) + same hot files as feedback PRD |

**Wave A** (PRDs 01–05): parallel-safe with your 1–7, data exists today — ship these first, in any order. 
**Wave B** (PRDs 06–07): touch `transitionPhase` / `archival.js` / `buildStateSummary` (the engine heart) — serialize after any 1–7 work that refactors the iteration/archival model; land 06 before 07.

## Cross-PRD coherence review

**Cross-cutting (reconcile when building in parallel):**

- README SECTION-INSERTION COLLISION (package-readme.js, 4-way): PRD 1 (## Captions) and PRD 3 (## Distribution) BOTH insert between ## Chapters (ends :98) and ## Target (:100); PRD 5 (## Aspect variants) also targets '~:98'; PRD 2 (## Posters) inserts after ## Thumbnail (:67). Verified section layout: Output :48, Thumbnail :56-67, Audio :70-82, Chapters :87-98, Target :100. Three sections at the identical :98-100 anchor will textually conflict. Each PRD claims 'composes cleanly' but none assigns a deterministic order. RECONCILE: fix a canonical section order (suggest Posters after Thumbnail; then Captions, Distribution, Aspect variants after Chapters in that order) and have all four authors cite the same insertion sequence.
- MANIFEST OBJECT LITERAL (package-output.js:345-413, 4-way additive spread): PRDs 1 (captions), 2 (posters), 3 (distribution), 5 (aspect_variants) all add conditional-spread keys to the SAME manifest literal. Conditional spreads ARE order-independent so semantics compose, but all four edit the same lines and will mechanically merge-conflict. All correctly keep manifest_version '1.2' (no bump) — consistent. Land them in a fixed key order (chapters, captions, distribution, posters, video, aspect_variants) so diffs stack.
- PACKAGE STATE SLOT (package-output.js:421-430) + summarizePackage (session-state.js:489-501), 3-way: PRDs 1 (captions_*_path), 2 (posters_dir/posters_count), 5 (variants[]) all add fields to the package slot AND to summarizePackage. Append-only but same two regions — order the additions identically in both files.
- transition-phase.js inputSchema.properties (:11-18), 2-way HARD collision: PRD 6 adds 5 args (revision_intent/target_layer/revision_label/prior_rating/preferred), PRD 7 adds promote_from_version, BOTH to the same properties block AND both edit the description at :8. Must merge into ONE properties object. Additionally both add arg-parsing + handling INSIDE transitionPhase (session-state.js:589-757) — PRD 6 threads feedback through archiveForIteration:676 + history:705-712; PRD 7 re-sequences the materialize/archive blocks. These overlap heavily; PRD 7 must rebase onto PRD 6's final shape.
- FAN-OUT MIRROR (import-deliverable.js) — captions + distribution both specify the mirror, loudness already exists: PRD 1 §4.4 specifies the per-deliverable .srt/.vtt mirror in the materialize loop (:273-324) with findTimeline(item.short_id) for fan-out scenes — CONFIRMED specified, but findTimeline is NOT currently imported (:20 imports only storyboardHasShorts, storyboardTimelines) so the destructure-add is required (PRD 1 notes this). PRD 3 §4.4 specifies the distribution mirror into deliverables/manifest.json regen (:352-360) via an extracted storyboard-schema helper distributionFromStoryboard(sb) — CONFIRMED specified. Loudness mirror (normalize:true) already lives at :286-297 — no PRD needs to add it. Both mirror PRDs touch the SAME materialize loop and the SAME deliverables/manifest.json regen literal — coordinate: PRD 1 adds per-record record.captions inside the loop; PRD 3 adds a top-level manifest.distribution in the regen. Distinct sinks, but adjacent edits.
- caption-sidecar shared module reuse: PRD 1 builds mcp/lib/caption-sidecar.js used by BOTH package-output.js and import-deliverable.js — this is the only genuinely-shared NEW module across the set and is reused consistently (one buildCaptionSidecar). No second copy. Good. BUT its timing bug (see per-prd) propagates to BOTH call sites if not fixed.
- distributionFromStoryboard helper home: PRD 3 puts the normalizer in storyboard-schema.js (exported) and layers chapters_paste_block in package-output.js. storyboard-schema.js is also the single highest-collision validator file (PRD 6 adds TARGET_LAYERS there too, though as a constant in archival.js per its own spec — NOT storyboard-schema). Confirm PRD 6's TARGET_LAYERS lands in archival.js (its stated home) not storyboard-schema, else a 2nd collision there.

**Sequencing conflicts:**

- package-output.js is the SINGLE hottest file: PRDs 1, 2, 3, 5 ALL edit the manifest literal (:345-413), the package state slot (:421-430), the history files count (:438), and session_artifacts_written (:476). These four cannot be developed on the same lines in parallel without serialization. Recommended serial order on a shared package branch: PRD 2 (posters, cheapest, no deps) → PRD 3 (distribution validator+emit) → PRD 1 (captions, depends on nothing but shares the loop) → PRD 5 (aspect-variants, lowest priority). Each rebases the manifest/slot/README edits onto the prior.
- session-state.js transitionPhase (:589-757) is edited by PRD 6 AND PRD 7 in overlapping regions. PRD 6 MUST land before PRD 7 (PRD 7 re-sequences the materialize/archive ordering and needs the settled history-build + the archiveForIteration call to stay callable). PRD 7's own §9 states this; enforce it.
- buildStateSummary (session-state.js:505-517): PRD 6 adds archived_versions[]; PRDs 1/2/5 add fields to summarizePackage (:489-501, a different function in the same file). PRD 4 (compare) READS archived_version_count (:517) but does not edit the summary. PRD 6 is the producer of archived_versions[] that PRD 4 and PRD 7's UX consume — land PRD 6's §4.5 summary shape before PRD 4/PRD 7 rely on it (PRD 4 gates on Array.isArray so it tolerates absence; PRD 7 needs it for UX). Freeze the archived_versions[] contract first.
- ITERATE batch dependency chain (stated on both sides, consistent): PRD 4 (compare, read-only, no engine-heart edits) FIRST → PRD 6 (feedback+labels, touches transitionPhase history-build) SECOND → PRD 7 (promote, re-sequences transitionPhase) LAST. PRD 7 declares the hard dep on PRD 4 (UX) and the soft-order dep on PRD 6 (history shape); PRD 6 declares it produces archived_versions[] for PRD 7; PRD 4 declares it ships independent of feedback[]. The chain is internally consistent.
- import-deliverable.js materialize loop (:273-324) + deliverables/manifest.json regen (:352-360): PRD 1 (per-record captions) and PRD 3 (top-level distribution) both edit here; PRD 6 does NOT touch this file. Serialize PRD 1 and PRD 3's import-side edits (adjacent, distinct sinks) — small merge but same two regions.
- MISSING SIBLING DEPENDENCY: PRD 1 (captions) declares itself 'foundational for the distribution sibling — land this first so it can rely on manifest.captions.' But PRD 3 (distribution) does NOT actually read manifest.captions anywhere in its spec — its chapters_paste_block reuses chaptersFromStoryboard, not captions. The stated PRD1→PRD3 dependency appears spurious; verify and drop it, or PRD 3 should state where it consumes manifest.captions. As written they are independent on the package side and only co-located in the import materialize loop.

**Per-PRD review severity:** caption-sidecar=major, poster-set=clean, distribution-metadata=minor, compare-iterations=minor, multi-aspect-crop=clean, iteration-feedback-and-labels=minor, promote-iteration=minor

