{
  "name": "video-vob",
  "version": "1.0.0",
  "description": "Open-source agent-driven video pipeline: drop raw footage in, answer five questions, walk out with a packaged short-form video. End-to-end FSM (ingest → intent → brief → storyboard → compose → preview → render → package → iterate) powered by hyperframes and ffmpeg.",
  "license": "Apache-2.0",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "mcp": "node mcp/server.js"
  }
}
