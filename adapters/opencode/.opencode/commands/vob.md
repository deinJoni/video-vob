---
description: Drive the video-vob pipeline — drop footage + a rough idea, get a packaged short
agent: vob
---
Start or resume a video-vob project.

Invocation input (may be empty, partial, classic-positional, or freeform prose): $ARGUMENTS

Resolve a `project_id`, a `source_path`, and any rough idea from that input per your **Argument parsing** rules, then drive the pipeline from the current phase. If the input is empty, ask for the footage path first, then proceed.
